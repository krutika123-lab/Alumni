<% layout("/layouts/boilerplate.ejs") %>

<!doctype html>
<html lang="en" class="h-full" data-theme="dark">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Events • Cummins College of Engineering, Nagpur</title>

  <!-- Tailwind -->
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: { container: { center: true, padding: "1rem" } }
      }
    }
  </script>

  <!-- Persist theme -->
  <script>
    (function () {
      const saved = localStorage.getItem("theme");
      const prefersDark = window.matchMedia("(prefers-color-scheme: dark)").matches;
      const theme = saved || (prefersDark ? "dark" : "light");
      document.documentElement.dataset.theme = theme;
      document.documentElement.classList.toggle("dark", theme === "dark");
    })();
  </script>

  <style>
    :root {
      --bg: #0b1220;
      --card: #111827;
      --muted: #93a2b1;
      --text: #e5e7eb;
      --ring: #243b53;
    }
    :root[data-theme="light"] {
      --bg: white;
      --card: #f8fafc;
      --muted: #475569;
      --text: #0f172a;
      --ring: #e2e8f0;
    }
    html {
      background: var(--bg);
      color: var(--text);
      transition: background 0.3s ease, color 0.3s ease;
    }
  </style>
</head>
<body class="min-h-full bg-[var(--bg)]">

  <!-- Header -->
  <header class="border-b border-[var(--ring)]/40 sticky top-0 backdrop-blur bg-[var(--bg)]/80 z-40">
    <div class="container flex items-center justify-between py-4 gap-4">
      <div class="flex items-center gap-3">
        <div class="w-10 h-10 rounded-xl bg-indigo-600/90"></div>
        <div>
          <h1 class="text-xl font-semibold">Cummins College of Engineering, Nagpur</h1>
          <p class="text-sm text-[var(--muted)]">Events & Activities Hub</p>
        </div>
      </div>
    </div>
  </header>

  <!-- Hero -->
  <section class="container py-10 text-center md:text-left">
    <div class="grid md:grid-cols-2 gap-6 items-center">
      <div>
        <h2 class="text-3xl md:text-4xl font-bold leading-tight">
          Discover New Mentorship Programs And Exciting Sessions By Alumni Of CCOEW Nagpur
        </h2>
        <p class="mt-3 text-[var(--muted)]">
         Visit here to ship in deep-dive workshops,exciting,informative lectures,sessions,meetings taken by alumni's of CCOEW Nagpur 
        </p>
        <div class="mt-6 flex gap-3 justify-center md:justify-start">
          <a href="#student-events" class="px-4 py-2 rounded-xl bg-indigo-600 hover:bg-indigo-500">Alumni- Mentorship Events</a>
          <a href="#new-event" class="px-4 py-2 rounded-xl border border-[var(--ring)]/60 hover:bg-[var(--card)]">Add Sesssions</a>
        </div>
      </div>
      <img
        class="rounded-2xl shadow-xl border border-[var(--ring)]/40 cursor-pointer"
        alt="Cummins College events"
        src="https://cumminscollege.edu.in/UserData/uploads/placed-students-2023-2024.jpeg"
      />
    </div>
  </section>
    <!-- Smart Search -->
    <div class="mb-6">
      <input 
        type="text" 
        id="studentEventSearch" 
        placeholder="Search sessions by title or description,alumni name..." 
        class="w-full sm:w-1/2 px-4 py-2 rounded-lg border border-[var(--ring)]/40 bg-[var(--card)] focus:outline-none focus:ring-2 focus:ring-indigo-500"
      />
    </div>

    <% if (eevent && eevent.length) { %>
      <div id="eventsGrid" class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
        <% eevent.forEach(e => { %>
          <article 
            class="event-card rounded-2xl text-white overflow-hidden border border-[var(--ring)]/40 bg-[var(--card)] flex flex-col"
            data-title="<%= e.title.toLowerCase() %>" 
            data-desc="<%= e.description.toLowerCase() %>"
          >
            <img class="h-44 w-full object-cover cursor-pointer" src="<%= e.image %>" alt="<%= e.title %>"/>
            <div class="p-4 flex-1 flex flex-col justify-between">
              <div>
                <h4 class="event-title text-lg font-semibold"><%= e.title %></h4>
                <p class="text-xs text-[var(--muted)] mt-0.5">Added by <span class="font-medium"><%= e.studentName %></span></p>
                <p class="event-desc text-sm text-[var(--muted)] mt-2"><%= e.description %></p>
                <p class="event-desc text-sm text-[var(--muted)] mt-2"><%= e.link %></p>
                <p class="event-desc text-sm text-[var(--muted)] mt-2"><%= e.date %></p>
                <p class="event-desc text-sm text-[var(--muted)] mt-2"><%= e.time %></p>
                <p class="text-[10px] text-[var(--muted)] mt-3">Posted <%= new Date(e.createdAt).toLocaleString() %></p>
              </div>
              <form action="/alumni/event/<%= e._id %>?_method=DELETE" method="POST" onsubmit="return confirm('Delete this event?');" class="mt-3">
                <button class="w-full px-3 py-2 text-sm rounded-lg bg-red-600 hover:bg-red-500">
                  Delete
                </button>
              </form>
            </div>
          </article>
        <% }) %>
      </div>
    <% } else { %>
      <p class="text-[var(--muted)]">No Alumni events yet. Be the first to add one!</p>
    <% } %>
  </section>

  <!-- New Event -->
  <section id="new-event" class="container py-10">
    <h3 class="text-2xl font-semibold mb-4">Add a Mentorship Session</h3>
    <form action="/alumni/event" method="POST" enctype="multipart/form-data"
      class="grid md:grid-cols-2 gap-6 bg-[var(--card)] p-6 rounded-2xl border border-[var(--ring)]/40">
      <div class="grid gap-4">
        <div>
          <label class="block text-sm mb-1">Title</label>
          <input name="title" required maxlength="140"
            class="w-full px-3 py-2 rounded-xl border border-[var(--ring)]/50 bg-transparent outline-none"
            placeholder="e.g., Arduino Bootcamp, AI"/>
        </div>
        <div>
          <label class="block text-sm mb-1">Alumni Name</label>
          <input name="studentName" required maxlength="100"
            class="w-full px-3 py-2 rounded-xl border border-[var(--ring)]/50 bg-transparent outline-none"
            placeholder="Your full name"/>
        </div>
        <div>
          <label class="block text-sm mb-1">Description</label>
          <textarea name="description" required maxlength="2000" rows="5"
            class="w-full px-3 py-2 rounded-xl border border-[var(--ring)]/50 bg-transparent outline-none"
            placeholder="Tell everyone what's happening..."></textarea>
        </div>
      </div>
      <div class="grid gap-4">
        <div>
          <label class="block text-sm mb-1">Upload Image</label>
          <input type="file" name="image" accept="image/*"
            class="w-full block text-sm file:mr-4 file:py-2 file:px-3 file:rounded-lg file:border-0
              file:bg-indigo-600 file:text-white hover:file:bg-indigo-500"/>
          <p class="text-xs text-[var(--muted)] mt-1">or paste an image URL below</p>
        </div>
        <input type="text" name="link" placeholder="Session link (eg. Google Meet)"
          class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:ring-2 focus:ring-purple-500">
        <input type="date" name="date" required
          class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:ring-2 focus:ring-purple-500">
        <input type="text" name="time" placeholder="Enter time" required
          class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:ring-2 focus:ring-purple-500">
        <div class="flex items-end">
          <button class="w-full px-4 py-2 rounded-xl bg-indigo-600 hover:bg-indigo-500">Submit Mentorship Session</button>
        </div>
      </div>
    </form>
  </section>

  <footer class="container py-1 text-center text-sm text-[var(--muted)]">
    © <%= new Date().getFullYear() %> Cummins College of Engineering, Nagpur. All rights reserved.
  </footer>

  <!-- Image Modal -->
  <div id="imageModal" class="hidden fixed inset-0 bg-black/80 flex items-center justify-center z-50">
    <span id="closeModal" class="absolute top-5 right-8 text-white text-3xl cursor-pointer">&times;</span>
    <img id="modalImage" class="max-h-[90%] max-w-[90%] rounded-xl shadow-lg" src="" alt="Enlarged view"/>
  </div>
  
  <script>
    // Smart Search
    document.getElementById("studentEventSearch").addEventListener("input", function () {
      const query = this.value.toLowerCase();
      const cards = document.querySelectorAll("#eventsGrid .event-card");
      cards.forEach(card => {
        const title = card.dataset.title;
        const desc = card.dataset.desc;
        if (title.includes(query) || desc.includes(query)) {
          card.style.display = "block";
        } else {
          card.style.display = "none";
        }
      });
    });

    // Theme Toggle
    const toggleBtn = document.getElementById("themeToggle");
    if (toggleBtn) {
      toggleBtn.addEventListener("click", () => {
        const html = document.documentElement;
        const newTheme = html.dataset.theme === "dark" ? "light" : "dark";
        html.dataset.theme = newTheme;
        html.classList.toggle("dark", newTheme === "dark");
        localStorage.setItem("theme", newTheme);
      });
    }

    // Image Modal
    document.querySelectorAll(".event-card img, section img").forEach(img => {
      img.addEventListener("click", () => {
        document.getElementById("modalImage").src = img.src;
        document.getElementById("imageModal").classList.remove("hidden");
      });
    });
    document.getElementById("closeModal").addEventListener("click", () => {
      document.getElementById("imageModal").classList.add("hidden");
    });
    document.getElementById("imageModal").addEventListener("click", (e) => {
      if (e.target.id === "imageModal") {
        document.getElementById("imageModal").classList.add("hidden");
      }
    });
    document.addEventListener("keydown", (e) => {
      if (e.key === "Escape") {
        document.getElementById("imageModal").classList.add("hidden");
      }
    });
  </script>
</body>
</html>
